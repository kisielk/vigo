package main

const (
	CONFIG_WRAP_LEFT  = true // Allow wrapping cursor to the previous line with 'h' motion.
	CONFIG_WRAP_RIGHT = true // Allow wrapping cursor to the next line with 'l' motion.

	TABSTOP_LENGTH            = 8
	VIEW_VERTICAL_THRESHOLD   = 5
	VIEW_HORIZONTAL_THRESHOLD = 10
)
